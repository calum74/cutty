#pragma once

namespace cutty::dynamic_detail
{
    void construct_reference_to_dynamic(const dynamic & src, dynamic & dest);
    void construct_reference_to_dynamic(dynamic & src, dynamic & dest);
}
